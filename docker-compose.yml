services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: contextual-ai-postgres

    environment:
      POSTGRES_DB: contextual_ai
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres

    ports:
      - "5433:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d contextual_ai"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data: